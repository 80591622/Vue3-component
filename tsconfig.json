{
  "compilerOptions": {
    // 基础编译配置
    "module": "ESNext", // 输出 ESNext 模块，适配现代构建工具
    "target": "ES2020", // 编译目标为 ES2020，兼顾兼容性与新特性
    "lib": [
      "ESNext",
      "DOM",
      "DOM.Iterable"
    ], // 编译时依赖的标准库
    "jsx": "preserve", // 保留 JSX 语法，由 Vue 编译器处理
    "jsxImportSource": "vue", // 指定 JSX 导入源为 Vue
    "types": [
      "@vue/runtime-dom"
    ], // 添加 Vue 运行时 DOM 类型
    "sourceMap": true, // 生成 SourceMap，便于调试
    // 模块解析配置
    "moduleResolution": "node", // 采用 Node 风格的模块解析
    "esModuleInterop": true, // 兼容 CommonJS 和 ES 模块的导入
    "allowSyntheticDefaultImports": true, // 允许从无默认导出的模块导入默认值
    "resolveJsonModule": true, // 支持导入 JSON 文件
    "baseUrl": ".", // 模块解析的基准路径
    "paths": { // 别名配置，统一管理跨包导入
      "@vue-nova/*": [
        "packages/*/src"
      ],
      "@vue-nova/utils": [
        "packages/utils/src"
      ]
    },
    // 严格类型检查
    "strict": true, // 开启所有严格类型检查选项
    "noImplicitAny": true, // 禁止隐式 any 类型
    "noImplicitThis": true, // 禁止隐式 this 类型
    "strictNullChecks": true, // 严格的 null/undefined 检查
    "strictFunctionTypes": true, // 严格的函数类型检查
    "strictBindCallApply": true, // 严格的 bind/call/apply 检查
    "strictPropertyInitialization": true, // 严格的属性初始化检查
    // 代码质量与风格
    "removeComments": false, // 保留注释，便于生成文档
    "preserveConstEnums": true, // 保留 const enum 定义
    "forceConsistentCasingInFileNames": true, // 强制区分文件名大小写
    "skipLibCheck": true, // 跳过第三方库类型检查，提升编译速度
    "isolatedModules": true, // 确保每个文件都可以被单独编译
    // 装饰器与高级特性
    "experimentalDecorators": true, // 启用装饰器语法（如 Vue Class Component）
    "emitDecoratorMetadata": true, // 生成装饰器元数据
    // 输出控制
    "declaration": true, // 生成 .d.ts 类型声明文件
    "declarationMap": true, // 生成声明文件的 SourceMap
    "outDir": "dist", // 编译输出目录
    "rootDir": "packages" // 源码根目录
  },
  // 项目文件范围
  "include": [
    "packages/**/*.ts",
    "packages/**/*.d.ts",
    "packages/**/*.tsx",
    "packages/**/*.vue",
    "packages/components/icon/src/test.vue"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "**/__tests__/**",
    "**/*.spec.ts",
    "**/*.test.ts"
  ]
}
